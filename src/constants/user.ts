/** 用户角色类型映射 */
export const USER_ROLE_MAP: Record<string, string> = {
  user: '普通用户',
  admin: '管理员',
  vip: 'VIP用户',
}

/** 用户角色类型标签色值映射 */
export const USER_ROLE_TAG_MAP: Record<string, string> = {
  user: '#108ee9',
  admin: '#cd201f',
  vip: '#faad14',
}
